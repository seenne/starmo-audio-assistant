<Window x:Class="StarAudioAssistant.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StarAudioAssistant.App"
        mc:Ignorable="d"
        Title="星辰音频助手"
        Width="1220"
        Height="760"
        MinWidth="1040"
        MinHeight="620"
        Background="#EEF5FF"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="BorderBrush" Value="#BCCADE" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#2F4362" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Border Margin="14" CornerRadius="12" BorderBrush="#D7E0EC" BorderThickness="1" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />
                <RowDefinition Height="52" />
                <RowDefinition Height="52" />
                <RowDefinition Height="54" />
                <RowDefinition Height="*" />
                <RowDefinition Height="36" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#FBFDFF">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Ellipse Width="10" Height="10" Fill="#1F4E94" Margin="0,0,10,0" />
                    <TextBlock Text="星辰音频助手" FontSize="20" FontWeight="SemiBold" Foreground="#1B3761" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <Button Content="最小化到托盘" Padding="12,5" Margin="0,0,8,0" Click="MinimizeToTrayButton_Click" />
                    <Button Content="关闭" Padding="14,5" Click="CloseButton_Click" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="定时任务" VerticalAlignment="Center" FontSize="24" FontWeight="SemiBold" Foreground="#16345F" />
                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           Foreground="#5C6B7F"
                           FontSize="13"
                           Text="调度策略：新任务到点立即切换，错过开始点则跳过" />
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="16,8,16,8">
                <Button Style="{StaticResource ToolbarButtonStyle}" Background="#1F4E94" Foreground="White" BorderBrush="#1F4E94" Content="新增任务" />
                <Button Style="{StaticResource ToolbarButtonStyle}" Content="编辑任务" />
                <Button Style="{StaticResource ToolbarButtonStyle}" Content="删除任务" />
                <Button Style="{StaticResource ToolbarButtonStyle}" Content="启用/停用" />
                <Button Style="{StaticResource ToolbarButtonStyle}" Content="测试播放" />
                <Button Style="{StaticResource ToolbarButtonStyle}" Content="导入配置" />
                <Button Style="{StaticResource ToolbarButtonStyle}" Content="导出配置" />
            </StackPanel>

            <Border Grid.Row="3" Margin="16,6,16,10" BorderBrush="#FFD7AD" BorderThickness="1" Background="#FFF6EC" CornerRadius="8" Padding="10,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center"
                               Foreground="#7A3F00"
                               FontSize="13"
                               Text="冲突提示：周二 23:00 时，‘周二深夜’将抢占当前播放任务（优先级同级，按列表顺序执行）。" />
                    <Button Grid.Column="1" Content="查看冲突详情" Padding="12,4" Margin="10,0,0,0" />
                </Grid>
            </Border>

            <Grid Grid.Row="4" Margin="16,0,16,10">
                <DataGrid x:Name="TaskGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          SelectionMode="Single"
                          RowHeaderWidth="0"
                          ItemsSource="{Binding TaskRows}"
                          MouseDoubleClick="TaskGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsEnabled}" Width="70" />
                        <DataGridTextColumn Header="任务名" Binding="{Binding Name}" Width="140" />
                        <DataGridTextColumn Header="时间" Binding="{Binding TimeRange}" Width="190" />
                        <DataGridTextColumn Header="规则" Binding="{Binding RuleType}" Width="140" />
                        <DataGridTextColumn Header="音频文件名" Binding="{Binding AudioFileName}" Width="200" />
                        <DataGridTextColumn Header="淡入淡出" Binding="{Binding Fade}" Width="120" />
                        <DataGridTextColumn Header="优先级" Binding="{Binding Priority}" Width="80" />
                        <DataGridTextColumn Header="下一次触发" Binding="{Binding NextTrigger}" Width="170" />
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Border Grid.Row="5" BorderBrush="#D7E0EC" BorderThickness="0,1,0,0" Background="#F4F8FF">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                    <TextBlock Foreground="#435974" FontSize="12" Text="当前播放：" />
                    <TextBlock Foreground="#1F3E6D" FontSize="12" FontWeight="SemiBold" Text="morning_focus.mp3" />
                    <TextBlock Margin="14,0" Foreground="#BCCADE" Text="|" />
                    <TextBlock Foreground="#435974" FontSize="12" Text="下一任务：" />
                    <TextBlock Foreground="#1F3E6D" FontSize="12" FontWeight="SemiBold" Text="Tue 23:00 周二深夜" />
                    <TextBlock Margin="14,0" Foreground="#BCCADE" Text="|" />
                    <TextBlock Foreground="#435974" FontSize="12" Text="音频设备：" />
                    <TextBlock Foreground="#1F3E6D" FontSize="12" FontWeight="SemiBold" Text="默认扬声器 (Realtek)" />
                    <TextBlock Margin="14,0" Foreground="#BCCADE" Text="|" />
                    <TextBlock Foreground="#435974" FontSize="12" Text="调度状态：" />
                    <TextBlock Foreground="#1F3E6D" FontSize="12" FontWeight="SemiBold" Text="运行中" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
